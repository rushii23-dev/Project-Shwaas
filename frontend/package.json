{
  "name": "shwaas-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "comment": "Leaflet is loaded from a CDN in index.html; the live app is the single vanilla-JS page (index.html + src/landing.js), so no runtime npm dependencies are needed.",
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
